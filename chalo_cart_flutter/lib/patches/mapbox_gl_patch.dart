/// This patch adds the missing hashValues function that was removed in newer Flutter versions
/// but is still used by the mapbox_gl package.
class MapboxGlPatch {
  static void apply() {
    // This is just a placeholder class to indicate that we need to patch the package
    // The actual patching will be done by modifying the package files directly
  }
}
